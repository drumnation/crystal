# Memory optimization for pnpm
# Reduce concurrent operations to lower memory usage
network-concurrency=1
fetch-retries=3
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

# Disable progress bar to reduce memory overhead
progress=false

# Use less memory during installation
prefer-offline=true
