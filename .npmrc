# Native module hoisting configuration
# Hoist most packages but exclude native modules to avoid build issues
public-hoist-pattern[]=*
public-hoist-pattern[]=!better-sqlite3
public-hoist-pattern[]=!@homebridge/node-pty-prebuilt-multiarch

# Dependency management for reliability
auto-install-peers=false
strict-peer-dependencies=true
prefer-frozen-lockfile=true
save-exact=true

# Engine and environment strictness
# Note: engine-strict commented out to avoid breaking existing setups
# Uncomment if you want to enforce exact Node.js/pnpm versions
# engine-strict=true